// Root vite config is unused — per-package builds (packages/*/vite.config.ts) handle
// the actual production build. This file is kept so that tooling that expects a root
// config (e.g. IDE integrations) has something to resolve against.
export default {};
